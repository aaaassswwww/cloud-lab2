apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: gomall
  labels:
    app: frontend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: frontend
